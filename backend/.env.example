# Copy to backend/.env and fill in. .env is gitignored -- never commit real secrets.
# Anything unset just falls back to stub/JSON behavior; set only what you need.

# --- Gemini (Layer 0 translator + the agent reasoner) ---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# --- Freesolo-trained model (Layer 1) ---
FREESOLO_API_KEY=
FREESOLO_MODEL=              # deployed <run-id>; defaults to Qwen/Qwen3.5-9B if unset (or --freesolo-model)
# Optional: defaults to https://api.freesolo.co. Set ONLY for a non-default deployment.
FREESOLO_BASE_URL=

# --- MongoDB run logging (MLH track). Atlas "Connect -> Drivers" string, password filled in ---
MONGODB_URI=
MONGODB_DB=agentview

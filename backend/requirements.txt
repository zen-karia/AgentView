# Stub mode (python3 run.py) needs NONE of these -- pure stdlib.
# Install these only when wiring real mode:
#   playwright        -> PlaywrightDriver (real browser). Then: playwright install chromium
#   google-genai      -> Gemini translator + reasoner (Layer 0)
#   anthropic         -> Claude prompting baseline + agent + MCP brain
#   openai            -> call the Freesolo-trained model (OpenAI-compatible endpoint, Layer 1)
#   pymongo           -> Mongo run logging (Lane B2)
# Freesolo training itself uses the `flash` CLI: uv tool install freesolo-flash
playwright>=1.44
google-genai>=0.3
anthropic>=0.117
openai>=1.0
pymongo>=4.7
certifi>=2024.0  # CA bundle so Atlas TLS works on macOS python.org builds
mcp>=1.2         # Playwright-MCP client + the AgentView MCP server (mcp_server.py)
uvicorn>=0.30    # ASGI server for the MCP HTTP transport

# Auth0 SPA configuration. The client ID is public; never add a client secret.
VITE_AUTH0_DOMAIN=dev-b8dqb1x583bd07a1.us.auth0.com
VITE_AUTH0_CLIENT_ID=K9uLButPJ1yTPsjG0vIvGL4aumCm4t4e

# Configure the Auth0 application with all three local URLs:
# Allowed Callback URLs: http://localhost:5173
# Allowed Logout URLs:   http://localhost:5173
# Allowed Web Origins:   http://localhost:5173
# Add the deployed HTTPS origin to each list before production use.

# Read-only benchmark API served by backend/api.py.
VITE_API_BASE_URL=http://127.0.0.1:8787

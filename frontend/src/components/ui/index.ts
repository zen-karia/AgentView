/* Shared visual primitives. Import from "@components/ui".
   Promote a component here only when both feature modules need it. */
import "./ui.css";

export { Card } from "./Card";
export { Badge } from "./Badge";
export { SegmentedControl } from "./SegmentedControl";
export { StatTile } from "./StatTile";
export { conditionColor, MUTED_MARK } from "./conditionColors";
export type { ThemeMode } from "./conditionColors";
